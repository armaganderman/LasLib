import { SectionDescriptor } from "./types";

/** Hamm metni satırlara bölüp Sections bulunacak */
export function parseSections(raw: string): SectionDescriptor[] {
   // TODO: ~ ile başlayan satırları yakala
   return [];
}